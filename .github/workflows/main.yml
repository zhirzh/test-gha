on: issue_comment
jobs:
  myjob:
    runs-on: ubuntu-latest
    env:
      GITHUB_CONTEXT: ${{ toJson(github) }}
    steps:
      - uses: lhotari/action-upterm@v1
#         run: |
#           echo start
#           echo $GITHUB_CONTEXT
#           echo done
