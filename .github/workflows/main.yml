on: issue_comment
jobs:
  myjob:
    runs-on: ubuntu-latest
    env:
      GITHUB_CONTEXT: ${{ toJson(github) }}
    steps:
      - run: |
          echo start
          hub pr checkout ${{ github.event.issue.number }}
          pwd
          ls
          echo done
